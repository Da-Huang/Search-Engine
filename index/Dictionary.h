#ifndef _DICTIONARY_H_
#define _DICTIONARY_H_

#include <string>
#include <map>
#include <ostream>
#include <Posting.h>
#include <vector>

using namespace std;

class Dictionary {
private:
	/*
	map<string, vector<Posting>> dict;
public:
	void add(const string &term, const Posting &posting);
	virtual ~Dictionary();
	*/
};

#endif
