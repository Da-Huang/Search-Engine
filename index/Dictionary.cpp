#include <Dictionary.h>

/*
void Dictionary::add(const string &term, const Posting &posting) {
	vector<Posting> &postingList = dict[term];
	const size_t N = postingList.size();
	if ( postingList[N - 1].docID == posting.docID )
		postingList[N - 1].merge(posting);
	else 
		postingList.push_back(posting);
}

size_t Dictionary::getTermID(const string &term) {
	return 0;
}

Dictionary::~Dictionary() {
}
*/
